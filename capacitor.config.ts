import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capAppDarkScreenStartup',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
